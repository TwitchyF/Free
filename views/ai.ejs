<!DOCTYPE html>
<html lang="en">
<head>
    <title>AI - NueApi</title>
    <link rel="icon" type="image/png" href="https://telegra.ph/file/e07e0fddc2a2967bd08d6.jpg"/>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Rest apis WhatsApp bot, Telegram bot, and Discord bot, free, has AI download IG feature and others">
    <meta name="tag" content="free api, nue-api, rest-api, azzapi">
    <style>
        body {
            background-color: #121212;
            color: #f8f9fa; /* White color for text */
            font-family: Arial, sans-serif;
        }

        .menu {
            display: flex;
            flex-direction: column;
            align-items: center;
            margin: 20px;
            padding: 10px;
            border: 1px solid #343a40; /* Dark color for border */
            background-color: rgba(0, 0, 0, 0.5); /* Semi-transparent black */
        }

        .menu-item {
            width: 100%;
            max-width: 500px;
            margin: 10px;
            padding: 20px;
            border: 1px solid #343a40;
            background-color: #1f1f1f; /* Darker background for items */
            border-radius: 8px;
        }

        .button {
            display: inline-block;
            margin: 10px 0;
            padding: 10px 20px;
            border: none;
            color: #f8f9fa;
            background-color: #007bff; /* Primary color */
            cursor: pointer;
            border-radius: 4px;
        }

        .button:disabled {
            background-color: #6c757d; /* Gray color when disabled */
            cursor: not-allowed;
        }

        .button:hover:enabled {
            background-color: #0056b3; /* Darker shade of primary color */
        }

        .home-button {
            margin: 20px;
            padding: 10px;
            border: none;
            background: transparent;
            cursor: pointer;
        }

        .home-button img {
            width: 40px;
            height: 40px;
        }

        .input-field {
            width: calc(100% - 20px);
            padding: 10px;
            margin: 5px 0;
            border: 1px solid #343a40;
            border-radius: 4px;
            background-color: #2c2c2c;
            color: #f8f9fa;
        }

        .input-field:focus {
            border-color: #007bff;
            outline: none;
        }
    </style>
</head>
<body>
    <div class="menu">
        <h1>Nue-APIs</h1>
        <button class="home-button" onclick="location.href='/dashboard'">
            <img src="https://static.vecteezy.com/system/resources/previews/024/029/940/original/home-icon-clipart-transparent-background-free-png.png" alt="Home">
        </button>
        <div class="menu-item">
            <h2>Alicia AI</h2>
            <p>Alicia AI yang cuek dan pemalas</p>
            <input type="text" class="input-field" id="alicia-text" placeholder="Masukkan perintah" oninput="checkInput('alicia-text', 'instagram-button')">
            <button class="button" id="instagram-button" disabled onclick="tryNow('/api/alicia?user=Ricky&text=', 'alicia-text')">Try Now</button>
        </div>
        <div class="menu-item">
            <h2>stable-diffusion SDXL</h2>
            <p>Ai pembuat gambar</p>
            <input type="text" class="input-field" id="diffusion" placeholder="Masukkan perintah" oninput="checkInput('diffusion', 'diff-button')">
            <button class="button" id="diff-button" disabled onclick="tryNow('/api/diff?prompt=', 'alicia-text')">Try Now</button>
        </div>
        <div class="menu-item">
            <h2>Gemini Pro</h2>
            <p>Ai fast dari google AI</p>
            <input type="text" class="input-field" id="tiktok-url" placeholder="Masukkan perintah" oninput="checkInput('tiktok-url', 'gemini-q')">
            <button class="button" id="gemini-q" disabled onclick="tryNow('/api/gemini?q=', 'tiktok-url')">Try Now</button>
        </div>
        <div class="menu-item">
            <h2>ChatGpt</h2>
            <p>Semua models ChatGPT</p>
            <input type="text" class="input-field" id="chatgpt-prompt" placeholder="Masukkan perintah" oninput="checkInput('chatgpt-prompt', 'youtube-button')">
            <button class="button" id="youtube-button" disabled onclick="tryNow('/api/gpt?model=gpt-3.5-turbo&prompt=', 'chatgpt-prompt')">Try Now</button>
        </div>
        <div class="menu-item">
            <h2>Translate</h2>
            <p>Terjemahkan bahasa menggunakan AI gpt</p>
            <input type="text" class="input-field" id="tr-text" placeholder="Masukkan Text" oninput="checkInput('tr-text', 'ytdl-button')">
            <button class="button" id="ytdl-button" disabled onclick="tryNow('/api/translate?lang=en&text=', 'tr-text')">Try Now</button>
        </div>
        <div class="menu-item">
            <h2>NueAI</h2>
            <p>Nue Ai cerdas dan fast</p>
            <input type="text" id="nueaiUrl" class="input-field" placeholder="Masukan perintah" oninput="checkInput('nueaiUrl', 'button')">
            <button id="button" class="button" disabled onclick="tryNow('/api/nueai?user=Ricky&text=', 'nueaiUrl')">Try Now</button>
        </div>
    </div>

    <script>
        function checkInput(inputId, buttonId) {
            const input = document.getElementById(inputId);
            const button = document.getElementById(buttonId);
            button.disabled = !input.value;
        }

        function tryNow(endpoint, inputId) {
            const input = document.getElementById(inputId);
            if (input.value) {
                location.href = endpoint + encodeURIComponent(input.value);
            }
        }
    </script>
</body>
</html>
